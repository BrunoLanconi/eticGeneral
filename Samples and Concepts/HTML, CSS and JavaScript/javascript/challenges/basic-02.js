// ==========================================
// DESAFIOS BÁSICOS DE JAVASCRIPT
// Foco: Arrays e Objects
// ==========================================

// ------------------------------------------
// DESAFIO 1 - Análise de código (Arrays)
// ------------------------------------------
// Dado o código JavaScript abaixo, o que será impresso no console?

const fruits = ["apple", "banana", "cherry"];
console.log(fruits[1]);

// ------------------------------------------
// DESAFIO 2 - Correção de código (Arrays)
// ------------------------------------------
// O código abaixo contém um erro. Corrija o código para que ele funcione corretamente.

const numbers = [1, 2, 3, 4, 5];
console.log(numbers[5]);

// ------------------------------------------
// DESAFIO 3 - Escrita de código (Arrays)
// ------------------------------------------
// Uma professora quer armazenar as notas de 5 alunos em um array.
// Crie um array chamado **notas** com 5 valores numéricos e imprima o array no console.

// ------------------------------------------
// DESAFIO 4 - Análise de código (Arrays)
// ------------------------------------------
// Dado o código JavaScript abaixo, o que será impresso no console?

const colors = ["red", "green", "blue"];
colors.push("yellow");
console.log(colors.length);

// ------------------------------------------
// DESAFIO 5 - Correção de código (Arrays)
// ------------------------------------------
// O código abaixo contém um erro. Corrija o código para que ele funcione corretamente.

const animals = ["dog", "cat", "bird"];
animals.push("fish", "hamster");
console.log(animals[5]);

// ------------------------------------------
// DESAFIO 6 - Escrita de código (Arrays)
// ------------------------------------------
// Crie um array chamado **meses** contendo os primeiros 3 meses do ano.
// Adicione "Abril" ao final do array usando o método push() e imprima o array completo.

// ------------------------------------------
// DESAFIO 7 - Análise de código (Objects)
// ------------------------------------------
// Dado o código JavaScript abaixo, o que será impresso no console?

const person = {
  name: "Maria",
  age: 25,
  city: "Lisboa"
};
console.log(person.age);

// ------------------------------------------
// DESAFIO 8 - Correção de código (Objects)
// ------------------------------------------
// O código abaixo contém um erro. Corrija o código para que ele funcione corretamente.

const car = {
  brand: "Toyota",
  model: "Corolla",
  year: 2020
};
console.log(car.color);

// ------------------------------------------
// DESAFIO 9 - Escrita de código (Objects)
// ------------------------------------------
// Crie um objeto chamado **livro** com as propriedades: titulo, autor e ano.
// Defina valores para cada propriedade e imprima o objeto no console.

// ------------------------------------------
// DESAFIO 10 - Análise de código (Objects)
// ------------------------------------------
// Dado o código JavaScript abaixo, o que será impresso no console?

const student = {
  name: "João",
  grade: 8.5
};
student.grade = 9.0;
console.log(student.grade);

// ------------------------------------------
// DESAFIO 11 - Correção de código (Objects)
// ------------------------------------------
// O código abaixo contém um erro. Corrija o código para que ele funcione corretamente.

const product = {
  name: "Notebook",
  price: 3000
};
product.stock = 10;
console.log(product.stock);

// ------------------------------------------
// DESAFIO 12 - Escrita de código (Objects)
// ------------------------------------------
// Um restaurante quer cadastrar um prato no sistema.
// Crie um objeto chamado **prato** com as propriedades: nome, preco e categoria.
// Imprima apenas o nome e o preço do prato.

// ------------------------------------------
// DESAFIO 13 - Análise de código (Arrays)
// ------------------------------------------
// Dado o código JavaScript abaixo, o que será impresso no console?

const numbers2 = [10, 20, 30, 40];
numbers2[1] = 25;
console.log(numbers2);

// ------------------------------------------
// DESAFIO 14 - Correção de código (Arrays)
// ------------------------------------------
// O código abaixo contém um erro. Corrija o código para que ele funcione corretamente.

const cities = ["Porto", "Lisboa", "Faro"];
cities.pop();
console.log(cities[2]);

// ------------------------------------------
// DESAFIO 15 - Escrita de código (Arrays)
// ------------------------------------------
// Crie um array chamado **tarefas** com 4 tarefas do dia (strings).
// Remova a última tarefa usando pop() e imprima o array atualizado.

// ------------------------------------------
// DESAFIO 16 - Análise de código (Array de Objects)
// ------------------------------------------
// Dado o código JavaScript abaixo, o que será impresso no console?

const users = [
  { name: "Ana", age: 28 },
  { name: "Carlos", age: 35 },
  { name: "Beatriz", age: 22 }
];
console.log(users[1].name);

// ------------------------------------------
// DESAFIO 17 - Correção de código (Array de Objects)
// ------------------------------------------
// O código abaixo contém um erro. Corrija o código para que ele funcione corretamente.

const products = [
  { name: "Mouse", price: 50 },
  { name: "Keyboard", price: 150 }
];
console.log(products[2].price);

// ------------------------------------------
// DESAFIO 18 - Escrita de código (Array de Objects)
// ------------------------------------------
// Uma escola precisa armazenar informações de 3 alunos (nome e nota).
// Crie um array chamado **alunos** contendo 3 objetos, cada um com as propriedades nome e nota.
// Imprima o nome do segundo aluno.

// ------------------------------------------
// DESAFIO 19 - Análise de código (Objects aninhados)
// ------------------------------------------
// Dado o código JavaScript abaixo, o que será impresso no console?

const company = {
  name: "TechCorp",
  address: {
    street: "Rua Principal",
    number: 100
  }
};
console.log(company.address.street);

// ------------------------------------------
// DESAFIO 20 - Correção de código (Objects aninhados)
// ------------------------------------------
// O código abaixo contém um erro. Corrija o código para que ele funcione corretamente.

const school = {
  name: "Escola ABC",
  location: {
    city: "Porto",
    country: "Portugal"
  }
};
console.log(school.address.city);

// ------------------------------------------
// DESAFIO 21 - Escrita de código (Objects)
// ------------------------------------------
// Crie um objeto chamado **filme** com as propriedades: titulo, diretor e anoLancamento.
// Adicione uma nova propriedade **duracao** (em minutos) ao objeto e imprima o objeto completo.

// ------------------------------------------
// DESAFIO 22 - Análise de código (Arrays)
// ------------------------------------------
// Dado o código JavaScript abaixo, o que será impresso no console?

const letters = ["a", "b", "c"];
letters.push("d");
letters.push("e");
console.log(letters[3]);

// ------------------------------------------
// DESAFIO 23 - Correção de código (Arrays)
// ------------------------------------------
// O código abaixo contém um erro. Corrija o código para que ele funcione corretamente.

const scores = [85, 90, 78, 92];
console.log(scores.length());

// ------------------------------------------
// DESAFIO 24 - Escrita de código (Arrays)
// ------------------------------------------
// Crie um array chamado **diasDaSemana** contendo os 7 dias da semana.
// Imprima no console o primeiro e o último dia da semana.

// ------------------------------------------
// DESAFIO 25 - Análise de código (Objects)
// ------------------------------------------
// Dado o código JavaScript abaixo, o que será impresso no console?

const phone = {
  brand: "Samsung",
  model: "Galaxy S21",
  price: 2500
};
delete phone.price;
console.log(phone.price);

// ------------------------------------------
// DESAFIO 26 - Correção de código (Objects)
// ------------------------------------------
// O código abaixo contém um erro. Corrija o código para que ele funcione corretamente.

const employee = {
  name: "Pedro",
  position: "Developer"
  salary: 5000
};
console.log(employee.salary);

// ------------------------------------------
// DESAFIO 27 - Escrita de código (Array de Objects)
// ------------------------------------------
// Uma loja quer cadastrar 3 produtos (nome e preço).
// Crie um array chamado **produtos** com 3 objetos.
// Imprima o preço do terceiro produto.

// ------------------------------------------
// DESAFIO 28 - Análise de código (Arrays)
// ------------------------------------------
// Dado o código JavaScript abaixo, o que será impresso no console?

const numbers3 = [5, 10, 15, 20, 25];
numbers3.splice(2, 1);
console.log(numbers3);

// ------------------------------------------
// DESAFIO 29 - Correção de código (Array de Objects)
// ------------------------------------------
// O código abaixo contém um erro. Corrija o código para que ele funcione corretamente.

const team = [
  { name: "Alice", role: "Designer" },
  { name: "Bob", role: "Developer" }
];
console.log(team[0].Name);

// ------------------------------------------
// DESAFIO 30 - Escrita de código (Objects e Arrays)
// ------------------------------------------
// Um supermercado quer registrar as vendas do dia.
// Crie um objeto chamado **vendas** com as propriedades: data (string), produtos (array com 3 nomes de produtos) e total (número).
// Imprima o segundo produto do array de produtos.

// ==========================================
// FIM DOS DESAFIOS
// ==========================================
