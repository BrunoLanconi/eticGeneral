"""
A folder grouping all necessary files to perform actions regarding simple_click project.

- <b>cli</b>: a folder grouping CLI necessary files.<br>
- <b>services</b>: a folder grouping main service related files.

This document provides all documentation regarding simple_click modules, which is a A sample project..

Software is delivered in a zipped archive and can be installed with pip package installer as described below:

```pip3 install simple_click-[version].tar.gz```

You can simply copy/paste in a command line:

```pip3 install simple_click-0.1.0.tar.gz```

Remove by:

```pip3 uninstall simple_click```
"""
